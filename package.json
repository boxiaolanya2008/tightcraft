{
  "name": "zhulong",
  "private": true,
  "version": "0.1.0",
  "description": "烛龙 Zhulong — 纯 TypeScript 自研 Agent 引擎(core)+ CLI + TUI(TypeScript monorepo)",
  "workspaces": [
    "core",
    "cli",
    "tui"
  ],
  "scripts": {
    "build": "npm run build --workspace core && npm run build --workspace cli && npm run build --workspace tui",
    "cli": "node cli/dist/index.js",
    "tui": "node tui/dist/index.js",
    "dev": "npm run build && node cli/dist/index.js run \"列出仓库根目录\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}